// Valid variable names
var thisIsFine;
var $noProblemHere;
var _underscore56;
var StartWithCapital;
var z5;

// Invalid variable names (commented out to prevent errors)
/*
var 999;
var 39Steps;
var &nope;
var single words only;
var yield;
*/

/* Further Adventures
 *
 * 1) Take a look at the errors in the
 *    error section of Console below.
 *
 * 2) Click Run in the console
 *    and see what errors appear there.
 *
 * 3) Delete the second block of variables
 *    one by one from the code.
 *    See how the errors change.
 *
 */

// Uncomment each line one by one to see the errors

// Invalid variable name: cannot start with a number
// var 999;

// Invalid variable name: cannot start with a number
// var 39Steps;

// Invalid variable name: cannot contain special characters like &
// var &nope;

// Invalid variable name: cannot contain spaces
// var single words only;

// Invalid variable name: 'yield' is a reserved keyword in JavaScript
// var yield;
